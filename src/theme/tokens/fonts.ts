import { Tokens } from '@pandacss/dev';

const fonts: Tokens['fonts'] = {
  body: {
    value: '"Figtree Variable", sans-serif',
  },
  mono: {
    value: '"Source Code Pro Variable", monospace',
  },
};

export default fonts;
